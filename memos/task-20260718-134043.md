## Decision memo: personal information on file

**Bottom line:** Very little — three items, all from the session harness, none from durable memory.

### What I have

| Item | Value | Source |
|---|---|---|
| Email | aarongitlin@gmail.com | `userEmail` context block injected by the harness |
| Git identity | "Aaron Gitlin" | `gitStatus` block (git config on this machine) |
| Working directory | `/Users/aarongitlin/hub`, macOS (Darwin 25.5.0), zsh | Environment block |

Inferable but not stated: your macOS username is `aarongitlin`, and you're working in a git repo on `master` with untracked `bin/` and `jobs/dispatcher/`.

### Conflicting / uncertain information

- **No conflicts found** — the three sources agree (email, git name, and home directory path all point to the same person).
- **Pronouns are unknown.** A name is not evidence of pronouns, so I'm using they/them for you and won't infer otherwise.
- **Nothing is verified.** All of this is self-reported machine config, not authenticated identity. Git `user.name` in particular is trivially set to anything.

### What I have *not* checked

I did not read `/Users/aarongitlin/.claude/projects/-Users-aarongitlin-hub/memory/` — no memories surfaced in this session's context, which suggests the index is empty, but I'd have to look to say so definitively. Want me to?

### Retention note

None of the above persists past this session on its own. Memory files are only written when you ask or when something clearly warrants saving — I haven't written any here.
